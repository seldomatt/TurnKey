# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion2.7/lib")
require 'motion/project/template/ios'

$:.unshift("./lib/")
require './lib/turnkey'

require 'bundler'
Bundler.require

Motion::Project::App.setup do |app|

  app.name = 'turnkey'
  app.specs_dir = "spec/"

end